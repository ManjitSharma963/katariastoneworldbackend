# Backend service on Railway — use PRIVATE networking for gateway proxy.
# Gateway service variable (set on inventory/gateway service):

# Replace "backend" with your actual Railway API service name:
BACKEND_URL=http://backend.railway.internal:8080

# If private DNS is unavailable during setup, temporary public URL:
# BACKEND_URL=https://your-api-service.up.railway.app

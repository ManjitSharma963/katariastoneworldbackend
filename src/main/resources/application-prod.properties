# Production profile — set SPRING_PROFILES_ACTIVE=prod on Railway.
# All secrets must come from environment variables (never commit real values).

spring.application.name=kataria-stone-world-apis
server.port=${PORT:8080}
server.address=0.0.0.0
server.forward-headers-strategy=framework

# Database — Railway injects MYSQL* when MySQL plugin is linked to this service.
# Override with SPRING_DATASOURCE_* if needed (e.g. local testing).
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${MYSQLUSER}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${MYSQLPASSWORD}}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# JWT — required in production
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:72000000}

# CORS — production: only your public site
cors.allow-all-origins=false
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://www.katariastoneworld.com,https://katariastoneworld.com}
spring.web.cors.allowed-origins=

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.katariastoneworld.apis=INFO
logging.level.org.springframework.web.servlet.resource=WARN

# Swagger (disable public UI in prod if desired)
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=${SWAGGER_UI_ENABLED:false}

# Health / metrics
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when_authorized
management.prometheus.metrics.export.enabled=false

# Accounting engine
accounting.engine.expense.enabled=true
accounting.engine.bill.enabled=true
accounting.engine.refund.enabled=true
accounting.engine.advance.enabled=true
accounting.engine.loan.enabled=true
accounting.engine.receivable.enabled=true
accounting.engine.payroll.enabled=true
accounting.engine.client.enabled=true
accounting.engine.budget.enabled=true
accounting.engine.adjustment.enabled=true

audit.report.application-name=Kataria Stone World Inventory
audit.report.company-name=Kataria Stone World
audit.report.report-version=1.0
audit.report.application-version=1.0.0
audit.report.fetch-size=500
audit.report.max-cell-chars=120

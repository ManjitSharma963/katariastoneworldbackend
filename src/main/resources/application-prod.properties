# Activate in production only: spring.profiles.active=prod (do not use profile "local" there)
#
# Production profile: disable API docs UI; tighten health details
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

management.endpoint.health.show-details=when_authorized

# Enable Flyway only when you intentionally migrate this environment (see application.properties comments)
# spring.flyway.enabled=true
